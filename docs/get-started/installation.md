`filonov` can be installed in a multiple ways.

/// tab | cli only
```
pip install filonov
```
///

/// tab | ui
```
pip install filonov[ui]
```
///

/// tab | api
```
pip install filonov[server]
```
///
/// tab | full
```
pip install filonov[all]
```
///
