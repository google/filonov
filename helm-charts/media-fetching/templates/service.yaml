apiVersion: v1
kind: Service
metadata:
  name: {{ include "media-fetching.fullname" . }}
  labels:
    {{- include "media-fetching.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "media-fetching.selectorLabels" . | nindent 4 }}
