apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "media-fetching.fullname" . }}-test-connection"
  labels:
    {{- include "media-fetching.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "media-fetching.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
