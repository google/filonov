apiVersion: v1
kind: Service
metadata:
  name: {{ include "filonov.fullname" . }}
  labels:
    {{- include "filonov.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "filonov.selectorLabels" . | nindent 4 }}
